name: vllm_cli
description: A command-line utility to interact with a vLLM service.
version: 0.1.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  http: ^1.5.0
  path: ^1.9.1
  remove_emoji: ^0.0.10
  sqlite3: ^2.9.3
  # path: ^1.9.0

dev_dependencies:
  build_runner: ^2.9.0
  lints: ^6.0.0
  mockito: ^5.5.1
  sqflite_common_ffi: ^2.3.6
  test: ^1.26.3
